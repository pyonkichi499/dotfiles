# 個人用GitHub設定（特定ディレクトリ配下で有効）
[includeIf "gitdir:~/work/private_github/"]
	path = ~/.gitconfig-personal

# dotfiles ディレクトリ配下でも個人用設定を読み込む
[includeIf "gitdir:~/dotfiles/"]
    path = ~/.gitconfig-personal

# [user]
# 	email = terauchi.hiroshi@cloud-ace.jp
# 	name = terauchi hiroshi
# 	signingkey = hoge
# [credential]
#     helper = store --file ~/.git-credentials

[color]
    diff = auto
    status = auto
    branch = auto
    ui = true

[alias]
    lol = log --oneline --graph
    loa = log --oneline --graph --all
    st = status -sb

[core]
	excludesfile = ~/.gitignore
	quotepath = false
[filter "lfs"]
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
[credential "https://source.developers.google.com"]
	helper = gcloud.sh
[commit]
	gpgsign = true
[tag]
	gpgSign = true
[init]
	defaultBranch = main

# マシン固有の設定（editor, http.cookiefile など）。例は .gitconfig.local.example
[include]
	path = ~/.gitconfig.local
